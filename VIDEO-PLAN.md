# МТК №24 — план кинематографичного видео-ролика

> **Финальный продукт = зацикленный видеофайл** для музейной проекции
> (≈4:17, ~257 с, см. `data/scenario.js`). Интерактив (`index.html`, перемотка,
> `tools/authoring.html`) — это **рабочий инструмент авторинга**, а НЕ конечная
> поставка. Всё ниже исходит из того, что на выходе — видео в петле.
>
> Парный к [README.md](README.md) / [HANDOFF.md](HANDOFF.md). Файл-владелец —
> основная сессия (сцена/UI). Идеи, помеченные `[модели]`, требуют согласования с
> сессией моделей.

---

## 0. Главное решение: НЕ меняем движок. Добавляем офлайн-рендер

Раз на выходе видео, нет смысла в Blender/Unreal — это выбросило бы уже сделанное
(камера по кадрам, FX, HUD, астро-свет, привязка данных). Вместо этого:

**three.js остаётся рендерером, но получает офлайн-режим** — покадровый рендер,
отвязанный от реального времени. Что это даёт:

- **Качество вместо скорости.** Не нужно держать 30–60 fps вживую — каждый кадр
  можно считать сколько угодно: супер-сэмплинг (рендер в 2× и даун-скейл = чистый
  антиалиасинг), тяжёлый постпроцессинг, много частиц, мягкие тени высокого
  разрешения. Бюджет GPU больше не ограничен «реальным временем».
- **HUD запекается бесплатно.** HUD у нас — это DOM-оверлей (титры, ГЗК-панель,
  таймлайн, часы, `#fx-flash`). Через **headless-браузер (Puppeteer)** `page.screenshot`
  снимает страницу целиком — canvas + HTML + шрифты Nolde/20 Kopeek — попиксельно
  точно. Никакой ручной композиции.
- **Детерминизм.** Один и тот же `renderTime` → один и тот же кадр. Идеально
  воспроизводимо, можно перезапускать, докручивать, рендерить по частям.

### Пайплайн рендера (P0 — включающая задача)

1. **Детерминизм времени.** Ввести в `scene.js` явный `renderTime` вместо
   `performance.now()`. Режим `?render=1`: не `requestAnimationFrame` по часам, а
   шаг `t += 1/FPS`, и глобальная функция `window.MTK24_seek(t)` / `MTK24_renderFrame(i)`.
2. **Captуре.** Headless Chrome (Puppeteer) грузит `index.html?render=1`, на каждый
   кадр `seek(i/FPS)` → `page.screenshot(PNG)`. ~7700 кадров на 257 с @30 fps.
   (Альтернатива в браузере — `CCapture.js`, но Puppeteer надёжнее и даёт супер-сэмплинг.)
3. **Сборка.** `ffmpeg` из PNG-секвенции → H.264/H.265 mp4. Аудио (см. §3) — отдельной
   дорожкой, мукс через `ffmpeg -i frames -i mix.wav`.
4. **Разрешение/форма.** Рендерить под нативное разрешение проектора и **аспект
   рабочего экрана ≈1.147** (из `tools/extract_shape.py`). Форма-трапеция с дугами
   (`shape-clip.js`) остаётся — контент внутри неё, край запечён в кадр.
5. **Скрипт** — новый `tools/render_video.mjs` (+ `tools/render.py` обёртка), зона
   основной сессии.

> ⚙️ **Бонус апгрейда three.js** (r123 → r17x) — разблокирует WebGPU, KTX2, свежие
> аддоны постпроцессинга. Не обязателен для старта, но желателен. Отдельная задача,
> аддоны должны совпасть по версии (зона: согласовать `lib/three/**`).

---

## 1. Постпроцессинг (P0) — «кино» из того же кадра

Сейчас рендер «голый» (нет `EffectComposer`). В офлайне можем позволить полный стек.
Всё — в `scene.js` (моя зона).

| Эффект | Что даёт | Усилие |
|---|---|---|
| **ACES Filmic tonemapping** | дорогой цвет, мягкие хайлайты латуни/куполов | XS (1 строка) |
| **Bloom** (`UnrealBloomPass`) | свечение огней, дульная вспышка «Авроры», лучи Смольного | S |
| **GTAO/SSAO** | контактное затенение — модели «садятся» на карту, перестают парить | M |
| **DoF / tilt-shift** (`BokehPass`) | эффект диорамы-макета; ведёт взгляд | M |
| **Зерно + виньетка + лёгкая хром. аберрация** | архивно-плёночная фактура 1917-го | S |
| **God rays** от Солнца у горизонта | объём, золотой час (высота Солнца уже считается) | M |
| **Цветокор LUT** под бренд (paper/brass/red/graphite) | единый «look» всего ролика | S |

---

## 2. Атмосфера и среда (P1) — холодный октябрьский Петроград

- **Нева живая** `[модели?]` — водная плоскость с отражениями (огни, «Аврора»),
  рябь-ударная волна от выстрела. Можно шейдером в `scene.js`, без новых GLB.
- **Объёмный туман/мгла** над водой, низкие облака, дым из труб.
- **Снег/изморось** частицами; искры и дым на событиях.
- **Свет как драматургия:** окна захваченных зданий загораются тёплым; прожекторы
  «Авроры»/Петропавловки метут по карте; дульная вспышка освещает весь город на кадр.
- **Толпы точками-роями** — рабочие/красногвардейцы стекаются по улицам к Зимнему
  (потоки частиц по геометрии улиц из `data/petrograd_osm.json`). Очень читаемо.

---

## 3. Звук (P1) — самый сильный апгрейд эффекта на единицу усилий

В видео звук **запекается в дорожку** (ещё проще, чем live-WebAudio из HANDOFF §1).

- Слои: ветер, дальний гул толпы, бой часов, щелчки телеграфа.
- **Холостой выстрел «Авроры»** — звуковая печать всего ролика, синхронно с FX.
- Сдержанная партитура под финал (нарастание к рассвету).
- Дикторский голос (`voFull` уже дословно прописан) — слотами или озвучка.
- Производство: микс стемов → `mix.wav` → мукс в mp4 через ffmpeg. Файл-кандидат —
  `tools/audio_mix/` + готовый `assets/audio/mix.wav` (вне репо по размеру, как `in/`).

---

## 4. Повествование — читаемость истории (P1)

- **Карта контроля как «Риск»:** районы тонируются по тому, кто держит; красное
  расползается за ночь — ощущается неизбежность переворота. (`becomes:red` уже
  монотонен по кадрам — расширить с точек на зоны.)
- **Счётчик баланса сил** ВРК ↔ Временное: тонкая шкала захваченного — даёт momentum.
- **Явная цепочка причина→следствие:** телеграф-пунктир из Смольного → здание
  загорается → анимируется маршрут → здание краснеет. Куски (FX) есть — связать в жест.
- **Камера-рассказчик:** грамматика планов — establishing (wide) → наезд (tight) →
  перекидка → отъезд на весь город в финале. `framing` в сценарии уже под это.
- **Цитаты как полноэкранные типографические карточки** (бренд-шрифты): напр.
  «Промедление смерти подобно».
- **Архивные врезки** (`assets/ill/*` уже есть) — полноэкранные растворения на
  эмоциональных битах, в паперной/полутоновой рамке под бренд.

---

## 5. Кульминация и финал (P2) — эффектность

- **Выстрел «Авроры» = пик:** полноэкранная вспышка → ударная волна по карте →
  секунда тишины → штурм Зимнего (сжимающееся кольцо — FX уже есть). Безошибочная вершина.
- **Финал-катарсис:** отъезд камеры, весь город красный, **рассвет 26 октября** —
  Солнце буквально встаёт по астрономии (`sunPos`). Титр «Советская власть». Развязка.
- **Мотивированные переходы:** чернильный bleed (FX «как чернила» есть), прорисовка
  телеграф-линии, «сгиб карты».
- **Лёгкий camera shake** на выстреле и крупных событиях.

---

## 6. Специфика «ролик в петле»

- **Арка есть начало и конец** (подполье → Советская власть/рассвет) — бесшовная
  петля неестественна. Решение: **fade из/в** (чёрный или paper) в первые/последние
  ~1–2 с, чтобы склейка петли дышала, а не дёргалась.
- Либо «дыхание» паузой на финальном титре перед перезапуском.
- **РЕШЕНО: финальный кадр — чистый кинокадр.** Только карта + титры/цитаты/врезки.
  Правая **ГЗК-панель и нижний таймлайн — рабочая обвязка авторинга**, в видео их нет.
  Реализация: в режиме `?render=1` скрывать `#tech`/ГЗК и таймлайн (CSS-класс
  `body.render` или флаг в `scene.js`); рабочий экран при рендере раскрывается на
  полный кадр под аспект проектора ≈1.147.

---

## Порядок работ (предлагаемый)

1. **P0 Офлайн-рендер** — `renderTime`/`seek` в `scene.js` + `tools/render_video.mjs`
   (Puppeteer → PNG → ffmpeg). Включающая задача: без неё остальное некуда «снимать».
2. **P0 Постпроцессинг** — ACES + bloom + GTAO + DoF + grain/vignette. Видно сразу.
3. **P1 Звук** — параллельно: стемы + мукс.
4. **P1 Атмосфера + читаемость** — туман/вода/толпы; зоны контроля; счётчик сил.
5. **P2 Кульминация/финал/переходы** — довести «Аврору», рассвет, fade-петлю.

Зона владения: пп. 1, 2, 5 и большая часть 4 — `scene.js`/`styles.css`/`index.html`/
`tools/*` (основная сессия). Новые ориентиры/вода как GLB — `[модели]`, через главный чат.
